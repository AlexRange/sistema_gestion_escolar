import 'package:flutter/material.dart';

class AppColors {
  static const primary    = Color(0xFF1565C0);
  static const secondary  = Color(0xFF0288D1);
  static const accent     = Color(0xFF00897B);
  static const error      = Color(0xFFD32F2F);
  static const warning    = Color(0xFFE65100);
  static const success    = Color(0xFF2E7D32);

  static const background = Color(0xFFF5F7FA);
  static const surface    = Color(0xFFFFFFFF);
  static const cardBg     = Color(0xFFFFFFFF);

  // Asistencia
  static const present    = Color(0xFF2E7D32);
  static const absent     = Color(0xFFD32F2F);
  static const late       = Color(0xFFE65100);
  static const justified  = Color(0xFF0288D1);

  // Calificaciones (semáforo)
  static const gradeHigh  = Color(0xFF2E7D32); // ≥ 8
  static const gradeMid   = Color(0xFFE65100); // 6-7.9
  static const gradeLow   = Color(0xFFD32F2F); // < 6
}
